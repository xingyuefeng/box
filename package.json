{
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "clean": "rm -rf node_modules ./packages/*/node_modules",
    "setup": "npm run clean && yarn install  && lerna bootstrap"
  
  }
}
